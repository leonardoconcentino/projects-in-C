#include <stdio.h> // includes stdIO library to be able to code in C

int main(){ // the main function. all the algorithm goes inside this "{}"

    printf("Hello, World!\n"); // print the message on the terminal to the user

    return 0; // finish the algorithm without any issues

} //closes the main function